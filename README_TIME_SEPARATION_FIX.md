# 時間計測の分離修正

この版では、演習時間を「試験回 × 午前/午後」ごとに別々に保存します。

## 変更点

- 第46回午前、第46回午後など、各パートごとに独立したタイマーを持ちます。
- メニューへ戻るとタイマーを一時停止し、途中から再開すると続きの時間から再開します。
- ブラウザを閉じて後日再開しても、閉じていた時間は加算されません。
- 履歴にも各回・午前午後ごとの所要時間を表示します。
- 「間違いだけ復習」は別セッションとして0秒から計測します。

GitHubにこのフォルダの内容を上書きしてpushしてください。
